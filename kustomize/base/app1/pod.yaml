apiVersion: v1
kind: Pod
metadata:
  name: app1
  namespace: default
spec:
  containers:
  - image: app1:latest
    command:
      - /bin/sh
      - "-c"
      - "sleep 60m"
    imagePullPolicy: IfNotPresent
    name: alpine
  restartPolicy: Always